services:
  - type: web
    name: sepsis-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python -m uvicorn fastapi_server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.9